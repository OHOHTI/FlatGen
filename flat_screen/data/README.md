# Data Directory

This cleaned repository keeps only lightweight data needed to seed the workflow:

- `scores_with_stotal_cleaned.txt`: cleaned training labels
- `stotal_config.json`: saved score-combination parameters
- `batches_2/`: batch CSV inputs for Materials Project scoring

Generated artifacts such as `graph_text_mp_pyg.pkl` and `data/scores/` are intentionally omitted.
